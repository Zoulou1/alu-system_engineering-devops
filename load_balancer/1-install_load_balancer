cript to configure Nginx with a custom header "X-Served-By" using the hostname of the server

# Update the package list and install Nginx if not installed
sudo apt-get update -y
sudo apt-get install nginx -y

# Configure custom header in the Nginx configuration
# Using /etc/nginx/conf.d/default.conf for the default server block
echo 'server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index index.html index.htm index.nginx-debian.html;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
        add_header X-Served-By $HOSTNAME;
    }
}' | sudo tee /etc/nginx/sites-available/default > /dev/null

# Link the configuration and reload Nginx
sudo ln -sf /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx

echo "Nginx configured with custom header X-Served-By: $HOSTNAME"

